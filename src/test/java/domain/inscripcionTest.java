package domain;

import domain.facultad.Alumno;
import domain.facultad.Inscripcion;
import domain.facultad.Materia;
import org.junit.Assert;
import org.junit.Test;

import java.util.List;
/*
public class inscripcionTest {

    @Test
    public void materiaAprobada() {
        Alumno alumno = new Alumno();
        Materia materia = new Materia();
        Materia materia1 = new Materia();
        Inscripcion inscripcion = new Inscripcion();

        materia.agregarCorrelativas(materia1);
        alumno.agregarMateriasAprobadas(materia1);
        alumno.agregarMateriasInscripcion(materia);

        Assert.assertTrue(inscripcion.aprobada(materia1));
    }
}
*/